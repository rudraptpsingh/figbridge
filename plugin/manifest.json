{
  "name": "Figbridge",
  "id": "figbridge",
  "api": "1.0.0",
  "main": "code.js",
  "ui": "ui.html",
  "editorType": ["figma", "dev"],
  "capabilities": ["inspect"],
  "documentAccess": "dynamic-page",
  "networkAccess": {
    "allowedDomains": ["http://localhost:7331"],
    "reasoning": "Figbridge talks only to a bridge the user runs on their own machine at http://localhost:7331. No external servers are contacted; no design data ever leaves the user's device. The bridge is an open-source Node process the user installs themselves via `npx figbridge-mcp init`. If it is not running, the plugin still works as a local export/spec tool — it simply shows an offline state."
  }
}
